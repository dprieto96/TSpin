
/*package test;

import org.junit.Test;

import junit.framework.TestCase;
import model.*;

public class ModelTest extends TestCase{
*/
	/*
	 *  Dejo esta clase de prueba con unos test de prueba sencillos para tenerlos como gu�a.
	 *  
	 *  Sugiero utilizar un tipo de assert que permita mostrar un mensaje con el resultado del test
	 *  tanto cuando sea favorable, como cuando haya que indicar alg�n tipo de error.
	 *  
	 *  Tambi�n he visto que podemos crear el objeto a probar directamente en el m�todo que hace el test,
	 *  es decir, crear el objeto Ficha ficha directamente en el m�todo testConstruirFicha().
	 *  
	 *  �sto tiene sentido si lo vemos como para qu� nos vamos a crear una Casilla y un Jugador si lo que queremos
	 *  probar es la Ficha.
	 */
	
/*	
	private Casilla casilla;
	private Jugador jugador;
	private Game game;
	
	public void init() {
		
		// TODO dar valores correctos a los args para poder hacer bien los test
		//this.ficha = new Ficha(null, 0);
		this.casilla = new Casilla(0, 0);
		this.jugador = new Jugador(0);
		this.game=new Game();
	}
	*/
	/**
	 * Prueba si las fichas se crean correctamente.
	 */
	/*public void testConstruirFicha() {
		init();
		assert(ficha != null);
	}
	
	
	/**
	 * Prueba si las casillas se crean correctamente.
	 */
/*	
	@Test
	public void testConstruirCasilla() {
		init();
		assert(casilla != null);
	}
	
	@Test
	 public void testInicioJugadores() {
		init();
		
		int resultado=game.getNumJugadores();
		int esperado=2;
		System.out.println(game.getNumJugadores());
		assertEquals(esperado, resultado);
		 
	 }*/
	/**
	 * Prueba si los jugadores se crean correctamente.
	 */
/*	@Test
	public void testConstruirJugador() {
		init();
		assert(jugador != null);
	}
	
	
}*/
